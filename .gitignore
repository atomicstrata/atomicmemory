# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
lib/
/target/
*.tsbuildinfo

# Test / coverage
coverage/
test-results/
playwright-report/
__pycache__/
*.py[cod]
.pytest_cache/
.nyc_output/
.vitest/

# Turborepo
.turbo/
# `turbo prune --docker` scratch output (also produced inside Dockerfile stages)
out/

# Root-level docs scratch — superpowers (and similar agent tooling) write
# plans/specs to `docs/superpowers/{plans,specs}/`. These are internal working
# artifacts and must never reach the public mirror via `public:sync`. Anchored
# to the repo root so package-level docs (packages/*/docs) stay tracked.
/docs/*
!/docs/cli-distribution.md
!/docs/cli-consolidation.md

# Logs
*.log
*.log.*
logs/
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# Env files (never commit secrets). Sanitized example templates are allowed.
.env
.env.*
.envrc
!.env.example
!.env*.example
# Smoke-test scratch env created by packages/core/scripts/docker-smoke-test.sh.
.env.docker-smoke-test

# Credentials / private keys
.npmrc
.yarnrc.yml
*.pem
*.key
*.p8
*.p12
*.pfx
id_rsa*
id_ed25519*
credentials.json
service-account*.json

# Cloud / deployment local state
.aws/
.azure/
.gcp/
.kube/
*.tfstate
*.tfstate.*
*.tfvars
*.tfvars.json
terraform.tfvars
docker-compose.override.yml

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo

# Local-only scratch
.tmp/
tmp/
temp/
*.tmp
*.temp
*.local
commit-message.txt
pr-description.md

# Local databases / dumps
*.db
*.sqlite
*.sqlite3
*.dump
*.backup

# Local caches / package artifacts
.cache/
.tsup/
*.tgz

# Local raw-storage filesystem data (RAW_STORAGE_LOCAL_FS_ROOT defaults).
data/raw-storage/
